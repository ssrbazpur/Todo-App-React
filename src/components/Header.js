const Header = (props) => {
  return (
    <header className="header">
      <h1>{props.title}</h1>
    </header>
  );
};

// CSS in Component
// const headingStyle = {
//   backgroundColor: "black",
// };
export default Header;
